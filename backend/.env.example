# Local example environment variables for backend
# Copy this to `.env` and fill in the real values for local testing.

# Database (used by local Docker compose)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=footdash

# JWT
JWT_SECRET=change-me

# Football API
FOOTBALL_API_MOCK=true
FOOTBALL_API_URL=
FOOTBALL_API_KEY=

# Redis (optional)
REDIS_URL=redis://localhost:6379

# Firebase Cloud Messaging (required for real push delivery)
# Fill these with your Firebase service account credentials (staging secrets recommended).
# NOTE: When pasting `FCM_PRIVATE_KEY` include literal newlines as `\n` or quote the value.
FCM_PROJECT_ID=
FCM_CLIENT_EMAIL=
FCM_PRIVATE_KEY=

# Optional overrides for smoke script
# Example: FAKE_FCM_TOKEN for a real device token to test delivery
FAKE_FCM_TOKEN=
# Backend (NestJS) - Environment Example
# Copy this file to .env and adjust values for your local/staging/prod environments.

# Option 1: Single DATABASE_URL (preferred when available)
# Format: postgresql://<user>:<password>@<host>:<port>/<database>
#DATABASE_URL=postgresql://postgres:postgres@localhost:5432/footdash

# Option 2: Individual DB_* variables (used if DATABASE_URL is not set)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=footdash

# Other common backend settings (add as needed)
# NODE_ENV=development
# LOG_LEVEL=debug
