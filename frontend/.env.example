# FootDash frontend environment template
# Copy to `.env` or export these variables in your shell before running Ionic/Angular commands.

# Default API base URL for the local backend
API_BASE_URL=http://localhost:4000

# Optional: production API override used by sync script (falls back to API_BASE_URL)
API_BASE_URL_PROD=https://api.yourdomain.com

# Optional: override the auth path if the backend changes
AUTH_PATH=/auth
